# Informe de auditoría — App_Diagnostico_Biopsias_Mama

- **Archivos auditados:** `C:\Users\jaimelopez\Documents\ATLANTIS UNIVERSITY 2026 - 2027\MAI540 - SEP 01 2026 - OCT 08 2026\MODULO 3\App_Diagnostico_Biopsias_Mama.ipynb`
- **Archivos de apoyo leídos:** ninguno (no hay `README.md`, `context.md` ni `DESCRIPCION.md` en la carpeta del proyecto; los archivos `.docx` presentes no son informes `.md` y no se leen como evidencia)
- **Columna objetivo:** `diagnostico` — codificación: 0 = maligno, 1 = benigno (evidencia: celda [4] (id=bbb980c2), `df['diagnostico'] = data.target  # 0 = maligno, 1 = benigno`)
- **Tipo de problema:** clasificación binaria (evidencia: celda [16] (id=881f59c8), `modelo = LogisticRegression(**kwargs)`)
- **Subgrupos:** ninguno (origen: argumento entregado explícitamente como "ninguna"; el dataset no contiene columnas de edad, sexo u otro atributo protegido evidente)
- **Skill:** auditoria-modelos v1.3 — fecha: 2026-09-24
- **Modificaciones al proyecto:** ninguna (auditoría de solo lectura)

## Resumen

| Verificación | Resultado |
|---|---|
| V1 Métricas reportadas | FALLA |
| V2 Partición de datos | FALLA |
| V3 Fuga de información | FALLA |
| V4 Disparidad entre subgrupos | NO SE PUEDE DETERMINAR |
| V5 Validez de datos de entrada | FALLA |
| V6 Variables no admisibles | PASA |

## Tabla de verificación

| ID | Verificación | Resultado | Evidencia | Observación |
|---|---|---|---|---|
| V1.1 | Rango válido | NO SE PUEDE DETERMINAR | celda [18] (id=93d6fc8f), salida: `ValueError: Input X contains infinity...` | El pipeline de entrenamiento (celda [18]) falla antes de imprimir ninguna métrica de proporción (accuracy, precisión, exhaustividad, F1, AUC). Las celdas posteriores (id=b004c030, id=9ce4b8ab, id=103aad66) tienen `"outputs": []` en el archivo, confirmando que nunca terminaron de ejecutarse. No hay ninguna métrica numérica de este tipo en ninguna salida guardada del notebook. |
| V1.2 | Consistencia con la matriz | NO SE PUEDE DETERMINAR | (sin salida) | No existe ninguna matriz de confusión en las salidas guardadas del notebook. |
| V1.3 | Clase positiva correcta | NO SE PUEDE DETERMINAR | celda [16] (id=881f59c8), `acc_train = accuracy_score(y_train, modelo.predict(X_train))` | El único cálculo de métrica en el código es `accuracy_score` global; no hay ninguna métrica por clase (`precision_score`, `recall_score`, `classification_report`, `pos_label`) que evaluar para verificar la etiqueta usada. No aplica. |
| V1.4 | No solo accuracy con desbalance | FALLA | celda [6] (id=cba9b56a), salida: `1    0.627` / `0    0.373`; celda [16] (id=881f59c8), `acc_train = accuracy_score(...)` y `acc_test = accuracy_score(...)` | Razón de desbalance = 0.627/0.373 = 1.6810 ≥ 1.5. El código de `entrenar_modelo` (celda 16) solo importa y calcula `accuracy_score` (train y test); no calcula exhaustividad, precisión, F1 ni matriz de confusión de ninguna clase. Por regla del criterio, esto es FALLA de diseño independientemente de que la celda 18 haya fallado antes de imprimir. |
| V1.5 | Métrica acorde al costo | FALLA | celda [0] (id=4769b499), `"estima... si un tumor es maligno o benigno... ayude a priorizar qué casos revisa primero un especialista"`; celda [16] (id=881f59c8), única métrica calculada: `accuracy_score` | El proyecto identifica una clase de interés (maligno, la que el especialista debe priorizar revisar) pero en ningún lugar del notebook declara explícitamente si un falso negativo (maligno clasificado como benigno) o un falso positivo cuesta más. La única métrica calculada es accuracy, que no penaliza específicamente ningún tipo de error — no corresponde a una métrica orientada al costo (exhaustividad o precisión) de la clase de interés. |
| V2.1 | División antes del preprocesamiento que aprende | PASA | celda [16] (id=881f59c8), `X_train, X_test, y_train, y_test = train_test_split(...)` seguido de `modelo = LogisticRegression(**kwargs); modelo.fit(X_train, y_train)` | El proyecto no tiene ningún escalador, imputador, codificador ni selector que se ajuste (`fit`/`fit_transform`) sobre los datos; el único `fit` es el del `LogisticRegression`, y ocurre después del `train_test_split`, solo sobre `X_train`/`y_train`. |
| V2.2 | Semilla fija | FALLA | celda [2] (id=b7d1693a), `RANDOM_STATE = 42` / `rng = np.random.RandomState(RANDOM_STATE)`; celda [4] (id=bbb980c2), `df['num_biopsias_previas'] = rng.randint(0, 4, size=n)` | `rng` se crea **una sola vez a nivel de módulo** (celda 2) y se reutiliza dentro de `cargar_datos()` (celda 4) en cada llamada, en vez de crearse dentro de la función. Esto hace que el generador avance entre llamadas: la celda [10] (id=fbef5102) reporta `"3) Filas no finitas: 140"` tras una llamada a `cargar_datos()`, y la celda [14] (id=7b2e899e) reporta `"Valores no finitos (inf/NaN) en la columna: 135 de 569"` tras otra llamada posterior — dos ejecuciones del mismo pipeline con la misma `RANDOM_STATE=42` produjeron conteos distintos (140 vs. 135), lo que confirma que los datos no son reproducibles pese a fijar la semilla. |
| V2.3 | Estratificación | FALLA | celda [16] (id=881f59c8), `train_test_split(X, y, test_size=0.25, random_state=RANDOM_STATE)` | Es clasificación binaria (V evidencia arriba) y el `train_test_split` no recibe `stratify=y`. |
| V2.4 | Misma partición para los modelos comparados | NO SE PUEDE DETERMINAR | celda [16] (id=881f59c8), `def entrenar_modelo(df, usar_class_weight=False)` | `entrenar_modelo` admite un parámetro `usar_class_weight`, pero en todo el notebook solo se invoca una vez (celda [18], `entrenar_modelo(df)`, con el valor por defecto). No hay ninguna comparación real entre modelos ejecutada. |
| V3.1 | Nada se ajusta con datos de prueba | PASA | celda [16] (id=881f59c8), `X_train, X_test, y_train, y_test = train_test_split(...)` | No hay transformador que se ajuste con datos de prueba (no hay escalador ni imputador), no hay selección de variables, no hay búsqueda de hiperparámetros (`GridSearchCV`) ni elección de umbral: el flujo es partición → `fit` solo sobre entrenamiento → `predict`. |
| V3.2 | Ninguna columna posterior a la predicción | FALLA | celda [4] (id=bbb980c2), `df['sesiones_tratamiento_programadas'] = np.where(df['diagnostico'] == 0, rng.randint(3, 9, size=n), 0)` | La columna predictora `sesiones_tratamiento_programadas` (incluida en `COLUMNAS_PREDICTORAS`, celda 16) se construye directamente a partir de la variable objetivo `diagnostico`: vale 0 siempre que el caso es benigno (1) y un valor entre 3 y 8 siempre que es maligno (0). Es una fuga de información por construcción — la columna codifica el diagnóstico casi perfectamente y no podría conocerse en el momento real de predecir un caso nuevo (de hecho, en `predecir_caso`, celda [24], se pide como un dato de entrada arbitrario). |
| V4.1 | Métrica por subgrupo | NO SE PUEDE DETERMINAR | (sin columna de subgrupo) | Se entregó explícitamente "ninguna" como variable de subgrupo, y el dataset (`load_breast_cancer`) no contiene columnas de edad, sexo, estado civil ni otro atributo protegido o proxy evidente. |
| V4.2 | Disparidad dentro del umbral | NO SE PUEDE DETERMINAR | (depende de V4.1) | V4.1 no es PASA. |
| V5.1 | Sin `inf`/`NaN` al entrenar | FALLA | celda [12] (id=b397da6d), `df['variabilidad_por_biopsia'] = df['mean texture'] / df['num_biopsias_previas']`, salida: fila 2 `21.25  0  inf`; celda [18] (id=93d6fc8f), salida: `ValueError: Input X contains infinity or a value too large for dtype('float64').` | La división `mean texture / num_biopsias_previas` no protege el denominador (que puede ser 0, ya que `num_biopsias_previas = rng.randint(0, 4, ...)` incluye 0), generando `inf` (140 filas según celda [10], 135 según celda [14] — ver V2.2). Existe una función `validar_datos` (celda [10], id=fbef5102) capaz de detectar esto, pero **nunca se llama** dentro del flujo real de entrenamiento (celda [18]: `cargar_datos()` → `construir_caracteristicas()` → `entrenar_modelo()`, sin paso de validación ni imputación intermedio); solo se usó manualmente en una celda de diagnóstico aparte. El resultado es el `ValueError` que impide entrenar el modelo. |
| V6.1 | Sin identificadores ni atributos prohibidos | PASA | celda [16] (id=881f59c8), `COLUMNAS_PREDICTORAS = ['mean texture', 'mean smoothness', 'mean symmetry', 'mean fractal dimension', 'texture error', 'smoothness error', 'symmetry error', 'variabilidad_por_biopsia', 'num_biopsias_previas', 'sesiones_tratamiento_programadas']` | Ninguna de las columnas es un identificador único (el dataset de `load_breast_cancer(as_frame=True)` no incluye columna de ID), y ningún archivo de apoyo del proyecto declara alguna variable como prohibida. (Nota: `sesiones_tratamiento_programadas` sí es problemática, pero por fuga de información — ver V3.2 — no por ser un identificador o atributo declarado prohibido). |

## Acciones recomendadas

1. **[V1.1]** Corregir primero V5.1 (el `ValueError` que impide entrenar) para que el pipeline complete y produzca salidas de accuracy/precisión/exhaustividad/F1 reales que puedan verificarse en [0, 1]. (prioridad: media)
2. **[V1.2]** Una vez el modelo entrene, calcular y guardar una matriz de confusión (`confusion_matrix`) junto con las métricas, para poder verificar que estas se derivan correctamente de ella. (prioridad: media)
3. **[V1.3]** Reportar métricas por clase (p. ej. `classification_report` o `recall_score`/`precision_score` con `pos_label` explícito) en vez de solo accuracy global, y dejar explícita la codificación usada al calcularlas. (prioridad: media)
4. **[V1.4]** Con un desbalance de 1.68, agregar al código el cálculo de exhaustividad y precisión (o F1) de la clase "maligno" (`pos_label=0`) y la matriz de confusión, no solo `accuracy_score`. (prioridad: alta)
5. **[V1.5]** Declarar explícitamente en el notebook (celda de texto) qué error es más costoso — perder un caso maligno (falso negativo) frente a marcar un caso benigno como sospechoso (falso positivo) — y elegir la métrica prioritaria en consecuencia (exhaustividad de la clase maligna si el FN es el error caro). (prioridad: alta)
6. **[V2.2]** Crear el generador aleatorio (`np.random.RandomState(RANDOM_STATE)` o `default_rng`) **dentro** de `cargar_datos()`, no como variable global de módulo, para que cada llamada con la misma semilla produzca exactamente los mismos datos. (prioridad: alta)
7. **[V2.3]** Agregar `stratify=y` a la llamada de `train_test_split` en `entrenar_modelo` (celda 16). (prioridad: alta)
8. **[V2.4]** Si se va a comparar `usar_class_weight=True` contra `False` (como sugiere la firma de `entrenar_modelo`), añadir en el notebook una celda que ejecute ambas variantes sobre la misma partición (misma semilla) y compare sus métricas explícitamente. (prioridad: media)
9. **[V3.2]** Eliminar `sesiones_tratamiento_programadas` de `COLUMNAS_PREDICTORAS` (o de los datos por completo): es un dato posterior al diagnóstico, construido a partir de `diagnostico`, y no existiría al momento de predecir un caso nuevo real. (prioridad: alta)
10. **[V4.1]** Si en el futuro se agregan atributos demográficos (edad, etc.) al dataset clínico real, calcular la métrica prioritaria por subgrupo antes de desplegar. (prioridad: media)
11. **[V4.2]** Depende de V4.1: una vez exista una variable de subgrupo, verificar que ningún subgrupo con ≥ 20 positivos se desvíe más de 0.10 de la métrica global. (prioridad: media)
12. **[V5.1]** Proteger la división en `construir_caracteristicas` (p. ej. excluir o imputar las filas con `num_biopsias_previas == 0`, o usar un denominador con un mínimo de 1), y/o llamar a `validar_datos` dentro del flujo de la celda 18 antes de `entrenar_modelo`, deteniendo o corrigiendo si se detectan `inf`/`NaN`. (prioridad: alta)

## Limitaciones de esta auditoría

- El notebook, ejecutado en el orden en que están las celdas, falla en la celda [18] (id=93d6fc8f) con un `ValueError` antes de entrenar el modelo; por lo tanto no hay salidas guardadas de accuracy, precisión, exhaustividad, F1, AUC ni matriz de confusión, lo que deja V1.1, V1.2, V1.3 y V2.4 en NO SE PUEDE DETERMINAR.
- Las salidas guardadas en el archivo no corresponden a una ejecución lineal única de arriba hacia abajo: la celda [10] (id=fbef5102, sección 3, "Validación de calidad de datos") llama a `construir_caracteristicas()` y usa `COLUMNAS_PREDICTORAS`, ambos definidos en celdas posteriores (12 y 16). Esto indica que el notebook fue ejecutado fuera de orden antes de guardarse, y que sus salidas son un rastro de esa ejecución fuera de orden, no del orden actual de las celdas.
- No se evaluó la disparidad entre subgrupos (V4) porque se entregó explícitamente "ninguna" variable de subgrupo y el dataset no trae atributos demográficos.
- No se leyó ningún archivo de apoyo (`README.md`/`context.md`/`DESCRIPCION.md`) porque no existe ninguno en la carpeta del proyecto; los `.docx` presentes no se consideran evidencia por la definición de esta Skill.
