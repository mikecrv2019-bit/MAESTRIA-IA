# Informe de auditoría — App_Diagnostico_Biopsias_Mama

- **Archivos auditados:** `MODULO 3\App_Diagnostico_Biopsias_Mama.ipynb`
- **Archivos de apoyo leídos:** ninguno (no se encontró `README.md`, `context.md`, `DESCRIPCION.md` ni informes `.md` propios de este proyecto en la carpeta `MODULO 3`; el `README.md` y los `AUDIT_REPORT` presentes en esa carpeta pertenecen al proyecto `iris-clasificadores-repo`, distinto al auditado)
- **Columna objetivo:** `diagnostico` — codificación: 0 = maligno, 1 = benigno (evidencia: celda [4] (id=bbb980c2), `df['diagnostico'] = data.target  # 0 = maligno, 1 = benigno`)
- **Tipo de problema:** clasificación binaria (evidencia: celda [16] (id=881f59c8), `modelo = LogisticRegression(**kwargs)`; celda [4] (id=bbb980c2), salida muestra únicamente valores 0 y 1 en `diagnostico`)
- **Subgrupos:** ninguno (origen: argumento entregado = "ninguna"; verificado además que las columnas del proyecto —las 30 mediciones de imagen de `load_breast_cancer`, `num_biopsias_previas` y `sesiones_tratamiento_programadas`, celda [4] (id=bbb980c2)— no incluyen atributos protegidos ni proxies como edad, sexo o estado civil)
- **Skill:** auditoria-modelos v(sin número de versión declarado en el encabezado de la Skill) — fecha: 2026-09-24
- **Modificaciones al proyecto:** ninguna (auditoría de solo lectura)

## Resumen

| Verificación | Resultado |
|---|---|
| V1 Métricas reportadas | FALLA |
| V2 Partición de datos | FALLA |
| V3 Fuga de información | FALLA |
| V4 Disparidad entre subgrupos | NO SE PUEDE DETERMINAR |
| V5 Validez de datos de entrada | FALLA |
| V6 Variables no admisibles | PASA |

## Tabla de verificación

| ID | Verificación | Resultado | Evidencia | Observación |
|---|---|---|---|---|
| V1.1 | Rango válido | NO SE PUEDE DETERMINAR | celda [18] (id=93d6fc8f), salida: `ValueError: Input X contains infinity or a value too large for dtype('float64').` | El pipeline de entrenamiento (celda [18]) falla antes de calcular `acc_train`/`acc_test`. No queda ninguna métrica de proporción (accuracy, precisión, exhaustividad, F1, AUC) en las salidas guardadas del notebook; la única salida con proporciones es la distribución de clases (celda [6]), que no es una métrica de desempeño del modelo. |
| V1.2 | Consistencia con la matriz | NO SE PUEDE DETERMINAR | (sin celda aplicable) | No existe ninguna matriz de confusión en el notebook: no se importa `confusion_matrix` ni se construye una tabla equivalente en ninguna celda. |
| V1.3 | Clase positiva correcta | NO SE PUEDE DETERMINAR | celda [16] (id=881f59c8), `acc_train = accuracy_score(y_train, modelo.predict(X_train))` | El único cálculo de desempeño es `accuracy_score` global (no por clase); no hay `classification_report`, `precision_score`, `recall_score` ni `pos_label` en ninguna celda, por lo que no hay ninguna métrica atribuida a una clase que se pueda verificar. |
| V1.4 | No solo accuracy con desbalance | FALLA | celda [6] (id=cba9b56a), salida: `1    0.627 / 0    0.373`; celda [16] (id=881f59c8), `acc_train = accuracy_score(...)` / `acc_test = accuracy_score(...)` | Razón de desbalance = 0.627/0.373 = 1.6810 ≥ 1.5. El notebook completo solo importa y usa `accuracy_score` (celda [2] (id=b7d1693a): `from sklearn.metrics import accuracy_score`); no se calcula exhaustividad, precisión, F1 ni matriz de confusión en ninguna celda, ni siquiera antes del error de la celda [18]. |
| V1.5 | Métrica acorde al costo | FALLA | celda [0] (id=4769b499), texto: `estima... si un tumor es maligno o benigno... para priorizar qué casos revisa primero un especialista`; celda [16] (id=881f59c8) | El proyecto identifica una clase de interés clínicamente evidente (maligno = detectar el caso que no se puede dejar pasar) pero no declara en ningún texto ni archivo de apoyo cuál error cuesta más (falso negativo vs. falso positivo). La única métrica del proyecto es accuracy global, que no prioriza ni exhaustividad ni precisión de la clase maligno. |
| V2.1 | División antes del preprocesamiento que aprende | PASA | celda [2] (id=b7d1693a), imports; celda [16] (id=881f59c8), `X_train, X_test, y_train, y_test = train_test_split(...)` seguido de `modelo.fit(X_train, y_train)` | El notebook no usa ningún transformador que aprenda de los datos (no hay `StandardScaler`, `SimpleImputer`, `OneHotEncoder`, `Pipeline` ni selector de variables en los imports de la celda [2] ni en el resto del notebook). El único ajuste (`fit`) es el del `LogisticRegression`, y recibe solo `X_train`/`y_train`. |
| V2.2 | Semilla fija | FALLA | celda [2] (id=b7d1693a), `RANDOM_STATE = 42` / `rng = np.random.RandomState(RANDOM_STATE)`; celda [4] (id=bbb980c2), `df['num_biopsias_previas'] = rng.randint(0, 4, size=n)` | `rng` se crea una sola vez a nivel global (celda [2]) y no dentro de `cargar_datos()`. La función `cargar_datos()` se invoca al menos 3 veces en el notebook (celda [4], celda [10] `df_crudo = cargar_datos()`, celda [18] `df = cargar_datos()`), y cada llamada avanza el estado del generador global, por lo que produce valores distintos de `num_biopsias_previas` y `sesiones_tratamiento_programadas` en cada ejecución pese a tener `random_state` fijo. `train_test_split` (celda [16]) sí usa `random_state=RANDOM_STATE` correctamente, pero el generador global comparado hace que el criterio falle. |
| V2.3 | Estratificación | FALLA | celda [16] (id=881f59c8), `X_train, X_test, y_train, y_test = train_test_split(X, y, test_size=0.25, random_state=RANDOM_STATE)` | Es clasificación binaria (ver tipo de problema) y la única partición del notebook no incluye `stratify=y`. |
| V2.4 | Misma partición para los modelos comparados | NO SE PUEDE DETERMINAR | celda [16] (id=881f59c8), `def entrenar_modelo(df, usar_class_weight=False)`; celda [18] (id=93d6fc8f), `modelo, acc_train, acc_test = entrenar_modelo(df)` | Hay un solo modelo entrenado en todo el notebook. El parámetro `usar_class_weight` permite una variante `class_weight='balanced'`, pero nunca se invoca con `usar_class_weight=True` en ninguna celda, así que no hay comparación real entre modelos que verificar. |
| V3.1 | Nada se ajusta con datos de prueba | PASA | celda [16] (id=881f59c8), `modelo.fit(X_train, y_train)` | No hay transformadores, selección de variables, `GridSearchCV` ni ajuste de umbral en el notebook; el único `fit` recibe exclusivamente datos de entrenamiento. |
| V3.2 | Ninguna columna posterior a la predicción | FALLA | celda [4] (id=bbb980c2), `df['sesiones_tratamiento_programadas'] = np.where(df['diagnostico'] == 0, rng.randint(3, 9, size=n), 0,)`; celda [16] (id=881f59c8), `COLUMNAS_PREDICTORAS = [..., 'sesiones_tratamiento_programadas']` | `sesiones_tratamiento_programadas` se construye directamente a partir de la variable objetivo `diagnostico` (`np.where(df['diagnostico'] == 0, ...)`): el número de sesiones de tratamiento programadas solo se conoce **después** de que el diagnóstico ya fue emitido, y sin embargo se usa como predictora en `COLUMNAS_PREDICTORAS`. Es fuga de información directa de la variable objetivo hacia una columna de entrada. |
| V4.1 | Métrica por subgrupo | NO SE PUEDE DETERMINAR | argumento de subgrupos = "ninguna"; celda [4] (id=bbb980c2), lista de columnas del `df` (mediciones de imagen, `num_biopsias_previas`, `sesiones_tratamiento_programadas`, `diagnostico`) | No se entregó ninguna variable de subgrupo y el proyecto no contiene atributos protegidos ni proxies evidentes (edad, sexo, estado civil, situación familiar) entre sus columnas. |
| V4.2 | Disparidad dentro del umbral | NO SE PUEDE DETERMINAR | (depende de V4.1) | V4.1 no es PASA, por lo que este subcriterio no se puede evaluar. |
| V5.1 | Sin `inf`/`NaN` al entrenar | FALLA | celda [12] (id=b397da6d), `df['variabilidad_por_biopsia'] = df['mean texture'] / df['num_biopsias_previas']`, salida muestra `inf` cuando `num_biopsias_previas = 0`; celda [10] (id=fbef5102), salida: `2) Con la derivada: {'variabilidad_por_biopsia': {'nulos': 0, 'infinitos': 140}}`; celda [18] (id=93d6fc8f), salida: `ValueError: Input X contains infinity...` | La división `mean texture / num_biopsias_previas` no protege el denominador (`num_biopsias_previas` puede ser 0, generado por `rng.randint(0, 4, size=n)` en celda [4]). Existe una función `validar_datos` (celda [10]) que detecta el problema, pero el pipeline de producción declarado en celda [17]-[18] ("Esto es lo que corre en producción") llama directamente a `cargar_datos → construir_caracteristicas → entrenar_modelo` sin invocar `validar_datos` ni tratar los infinitos, y por eso el entrenamiento falla con `ValueError`. |
| V6.1 | Sin identificadores ni atributos prohibidos | PASA | celda [16] (id=881f59c8), `COLUMNAS_PREDICTORAS = ['mean texture', 'mean smoothness', 'mean symmetry', 'mean fractal dimension', 'texture error', 'smoothness error', 'symmetry error', 'variabilidad_por_biopsia', 'num_biopsias_previas', 'sesiones_tratamiento_programadas']` | Ninguna de las predictoras es un identificador único; `load_breast_cancer(as_frame=True).frame` (celda [4]) no incluye columna de identificador de paciente. No hay archivo de apoyo que declare atributos prohibidos para este proyecto. (La fuga de `sesiones_tratamiento_programadas` ya está registrada en V3.2, no es un identificador ni un atributo declarado prohibido). |

## Acciones recomendadas

1. **[V1.1]** Corregir primero V5.1 (el `ValueError` que impide el entrenamiento) y volver a ejecutar el notebook completo, guardando las salidas de `acc_train`/`acc_test`, para poder verificar que las métricas caigan en [0, 1]. (prioridad: media)
2. **[V1.2]** Agregar `confusion_matrix(y_test, modelo.predict(X_test))` a la celda de entrenamiento y mostrarla como salida guardada, para poder recalcular las métricas reportadas desde la matriz. (prioridad: media)
3. **[V1.3]** Reportar métricas por clase (p. ej. `classification_report(y_test, y_pred, target_names=['maligno', 'benigno'])`) en lugar de solo `accuracy_score`, dejando explícito qué etiqueta corresponde a cada clase. (prioridad: media)
4. **[V1.4]** Agregar exhaustividad y precisión (o F1) de la clase maligno, además de la matriz de confusión, ya que la razón de desbalance (1.68) supera el umbral de 1.5 y accuracy sola no es suficiente. (prioridad: alta)
5. **[V1.5]** Declarar explícitamente en el notebook (o en un `context.md` del proyecto) si un falso negativo (decir "benigno" cuando es maligno) o un falso positivo cuesta más, y priorizar la métrica correspondiente (exhaustividad de maligno si el FN es el error grave). (prioridad: alta)
6. **[V2.2]** Mover `rng = np.random.RandomState(RANDOM_STATE)` dentro de `cargar_datos()` (o pasar la semilla como parámetro y crear el generador ahí) para que cada llamada a `cargar_datos()` produzca el mismo dataset sintético de forma reproducible. (prioridad: alta)
7. **[V2.3]** Agregar `stratify=y` a la llamada de `train_test_split` en `entrenar_modelo`. (prioridad: alta)
8. **[V2.4]** Si el propósito del parámetro `usar_class_weight` es comparar dos variantes del modelo, agregar la celda que efectivamente llama `entrenar_modelo(df, usar_class_weight=True)` sobre la misma partición y mostrar ambos resultados; si no se va a comparar, documentar por qué existe el parámetro. (prioridad: media)
9. **[V3.2]** Eliminar `sesiones_tratamiento_programadas` de `COLUMNAS_PREDICTORAS` (o de las columnas construidas a partir de `diagnostico`): es información que solo existe después del diagnóstico y no puede usarse como entrada del modelo en producción. (prioridad: alta)
10. **[V4.1]** Ninguna acción pendiente: no se entregaron subgrupos y el proyecto no contiene atributos protegidos evidentes. Si en el futuro se agregan columnas demográficas (p. ej. edad de la paciente), evaluar disparidad entonces. (prioridad: media)
11. **[V4.2]** Depende de V4.1; sin acción mientras no haya variable de subgrupo. (prioridad: media)
12. **[V5.1]** Proteger el denominador en `construir_caracteristicas` (p. ej. excluir o imputar las filas con `num_biopsias_previas == 0`, o usar `np.where(df['num_biopsias_previas'] == 0, np.nan, df['mean texture'] / df['num_biopsias_previas'])` seguido de una imputación explícita) y hacer que el pipeline de producción (celda [18]) llame a `validar_datos` antes de entrenar, deteniéndose o corrigiendo si encuentra infinitos. (prioridad: alta)

## Limitaciones de esta auditoría

- El notebook nunca completa una ejecución exitosa: el pipeline declarado como "lo que corre en producción" (celda [18], id=93d6fc8f) termina en `ValueError` por los valores infinitos de `variabilidad_por_biopsia`. En consecuencia, las celdas posteriores (`b004c030` coeficientes, `9ce4b8ab` guardar_modelo, `103aad66` predecir_caso) tienen `outputs: []` en el archivo — nunca se ejecutaron con éxito — y no hay evidencia de que el modelo se haya guardado (`modelo_biopsias.joblib`) ni de que la función `predecir_caso` haya devuelto un resultado.
- La celda [10] (id=fbef5102, sección "Validación de calidad de datos") usa `construir_caracteristicas` y `COLUMNAS_PREDICTORAS`, que se definen recién en las celdas [12] y [16], posteriores en el orden del archivo. Su salida guardada (`execution_count` interno 22 en la salida de la celda [12], frente a `execution_count` 18 en la salida de la celda [4]) es compatible con que esas salidas provienen de una sesión de kernel donde las celdas se ejecutaron en un orden distinto al que tienen hoy en el archivo, no de una corrida limpia de arriba hacia abajo. Esto significa que, si se reordenara la ejecución tal como está el archivo hoy, la celda [10] fallaría con `NameError` antes de llegar siquiera al `ValueError` de la celda [18]. No se ejecutó el notebook para confirmarlo (regla de la Skill); se deja como limitación, no como veredicto.
- No se encontró ningún archivo de apoyo (`README.md`, `context.md`, `DESCRIPCION.md`) específico de este proyecto en `MODULO 3`, por lo que V1.5 se evaluó únicamente con lo que el propio notebook declara en sus celdas de texto.
- No hay salidas guardadas de accuracy, matriz de confusión ni reporte por clase en ningún punto del notebook, lo que deja V1.1, V1.2 y V1.3 en NO SE PUEDE DETERMINAR en lugar de FALLA o PASA.
