# Informe de auditoría — App_Diagnostico_Biopsias_Mama

- **Archivos auditados:** `C:\Users\jaimelopez\Documents\ATLANTIS UNIVERSITY 2026 - 2027\MAI540 - SEP 01 2026 - OCT 08 2026\MODULO 3\App_Diagnostico_Biopsias_Mama.ipynb`
- **Archivos de apoyo leídos:** ninguno (no hay `README.md`, `context.md` ni `DESCRIPCION.md` en la carpeta del proyecto; los `.docx` presentes no son archivos `.md` y los `AUDIT_REPORT*.md` existentes no se leen por regla de la Skill)
- **Columna objetivo:** `diagnostico` — codificación: 0 = maligno, 1 = benigno (evidencia: celda [4] (id=bbb980c2): `df['diagnostico'] = data.target  # 0 = maligno, 1 = benigno`)
- **Tipo de problema:** clasificación binaria (evidencia: celda [16] (id=881f59c8): `modelo = LogisticRegression(**kwargs)`; celda [6] (id=cba9b56a), salida: `diagnostico\n1    0.627\n0    0.373`)
- **Subgrupos:** ninguno (origen: argumento entregado explícitamente como "ninguna"; el dataset no contiene columnas de atributos protegidos o proxies — sexo, edad, estado civil — más allá de las variables de imagen y las dos columnas clínicas agregadas)
- **Skill:** auditoria-modelos v1.1 — fecha: 2026-09-24
- **Modificaciones al proyecto:** ninguna (auditoría de solo lectura)

## Resumen

| Verificación | Resultado |
|---|---|
| V1 Métricas reportadas | FALLA |
| V2 Partición de datos | FALLA |
| V3 Fuga de información | FALLA |
| V4 Disparidad entre subgrupos | NO SE PUEDE DETERMINAR |
| V5 Validez de datos de entrada | FALLA |
| V6 Variables no admisibles | PASA |

## Tabla de verificación

| ID | Verificación | Resultado | Evidencia | Observación |
|---|---|---|---|---|
| V1.1 | Rango válido | NO SE PUEDE DETERMINAR | Ninguna celda del notebook muestra en su salida un valor de accuracy, precisión, exhaustividad, F1 o AUC. La única celda que calcula esas métricas (celda [18], id=93d6fc8f) falla con `ValueError` antes de imprimirlas. | No hay métricas numéricas en salidas guardadas. |
| V1.2 | Consistencia con la matriz | NO SE PUEDE DETERMINAR | En ninguna celda del notebook se construye una matriz de confusión (no se importa `confusion_matrix` en celda [2], id=b7d1693a, ni se usa en ningún otro lugar). | No hay matriz de confusión en las salidas. |
| V1.3 | Clase positiva correcta | NO SE PUEDE DETERMINAR | La codificación de `diagnostico` sí se conoce (0=maligno, 1=benigno, celda [4]), pero no existe ninguna salida guardada con una métrica atribuida a una clase específica (ni `classification_report` ni `pos_label`). | No se reportó ninguna métrica por clase; nada que evaluar contra la codificación. |
| V1.4 | No solo accuracy con desbalance | FALLA | Desbalance: celda [6] (id=cba9b56a), salida `diagnostico\n1    0.627\n0    0.373` → 0.627/0.373 = 1.6810 ≥ 1.5. Único import de métricas en todo el notebook: celda [2] (id=b7d1693a): `from sklearn.metrics import accuracy_score` (no se importa `precision_score`, `recall_score`, `f1_score`, `confusion_matrix` ni `roc_auc_score` en ningún lugar). `entrenar_modelo` (celda [16], id=881f59c8) solo calcula `accuracy_score`. | Con desbalance ≥1.5, el diseño del código nunca calcula exhaustividad, precisión, F1 ni matriz de confusión — únicamente accuracy, incluso si la ejecución no hubiera fallado. |
| V1.5 | Métrica acorde al costo | FALLA | Celda [0] (id=4769b499): `"La idea es que el modelo ayude a priorizar qué casos revisa primero un especialista"` identifica una clase de interés (maligno, la que se prioriza para revisión). Ningún archivo de apoyo ni celda declara explícitamente que un falso negativo (maligno predicho como benigno) cuesta más que un falso positivo. La única métrica calculada (accuracy, ver V1.4) no prioriza exhaustividad de la clase maligna. | Hay clase de interés identificable pero no se declara el costo de los errores, y la métrica única no corresponde a ese costo. |
| V2.1 | División antes del preprocesamiento que aprende | PASA | `entrenar_modelo` (celda [16], id=881f59c8) solo hace `train_test_split(...)` seguido de `LogisticRegression(**kwargs).fit(X_train, y_train)`. `construir_caracteristicas` (celda [12], id=b397da6d) es una operación aritmética por fila (`mean texture / num_biopsias_previas`), sin `fit`. No hay `StandardScaler`, `Imputer`, `OneHotEncoder` ni selector en todo el notebook. | El proyecto no tiene preprocesamiento que aprenda de los datos. |
| V2.2 | Semilla fija | FALLA | Celda [2] (id=b7d1693a): `RANDOM_STATE = 42` y `rng = np.random.RandomState(RANDOM_STATE)` creado en el ámbito global. `cargar_datos` (celda [4], id=bbb980c2) usa ese `rng` global (`rng.randint(...)`) en vez de crear su propio generador. `cargar_datos()` se invoca 3 veces (celdas [4], [10] vía `df_crudo = cargar_datos()`, y [18]), y cada llamada consume del mismo generador compartido, produciendo columnas distintas en cada invocación. | `train_test_split` sí recibe `random_state=RANDOM_STATE` (celda [16]), pero el generador de las columnas clínicas es global y avanza entre llamadas — no es reproducible entre ejecuciones de `cargar_datos()`. |
| V2.3 | Estratificación | FALLA | Celda [16] (id=881f59c8): `train_test_split(X, y, test_size=0.25, random_state=RANDOM_STATE)` — sin `stratify=y`, en un problema de clasificación binaria con desbalance (ver V1.4). | Es clasificación y la partición no está estratificada. |
| V2.4 | Misma partición para los modelos comparados | NO SE PUEDE DETERMINAR | `entrenar_modelo(df, usar_class_weight=False)` (celda [16]) admite un parámetro `usar_class_weight`, pero en todo el notebook solo se invoca una vez, con el valor por defecto (celda [18], id=93d6fc8f: `modelo, acc_train, acc_test = entrenar_modelo(df)`). | Hay un solo modelo entrenado, sin comparación entre variantes. |
| V3.1 | Nada se ajusta con datos de prueba | PASA | `COLUMNAS_PREDICTORAS` (celda [16]) es una lista fija en código, no derivada de una selección posterior a ver resultados de prueba. La única partición (`train_test_split`) ocurre dentro de `entrenar_modelo`, inmediatamente antes del único `fit` del notebook. No hay búsqueda de hiperparámetros, selección de umbral ni CV. | No se rastrea ninguna decisión (ajuste, selección de variables, hiperparámetro o umbral) tomada con datos de prueba. |
| V3.2 | Ninguna columna posterior a la predicción | FALLA | Celda [4] (id=bbb980c2): `df['sesiones_tratamiento_programadas'] = np.where(df['diagnostico'] == 0, rng.randint(3, 9, size=n), 0)`. Esta columna, incluida entre las predictoras (`COLUMNAS_PREDICTORAS`, celda [16]), se construye directamente a partir de la variable objetivo `diagnostico`: vale 0 siempre que el caso es benigno y un valor ≥3 siempre que es maligno, replicando el diagnóstico. | Columna sospechosa: `sesiones_tratamiento_programadas`, generada con `np.where(df[objetivo] == …)` — coincide exactamente con el ejemplo de fuga de la Skill. No es una variable que se conocería antes de emitir el diagnóstico. |
| V4.1 | Métrica por subgrupo | NO SE PUEDE DETERMINAR | El argumento de subgrupos se entregó como "ninguna" y el dataset (columnas de imagen + `num_biopsias_previas` + `sesiones_tratamiento_programadas` + `diagnostico`) no contiene sexo, edad, estado civil ni otro atributo protegido o proxy evidente. | No hay ninguna variable de subgrupo entregada ni encontrada en el proyecto. |
| V4.2 | Disparidad dentro del umbral | NO SE PUEDE DETERMINAR | Depende de V4.1. | V4.1 no es PASA. |
| V5.1 | Sin `inf`/`NaN` al entrenar | FALLA | Celda [12] (id=b397da6d): `df['variabilidad_por_biopsia'] = df['mean texture'] / df['num_biopsias_previas']` sin proteger el denominador. Celda [10] (id=fbef5102), salida: `2) Con la derivada: {'variabilidad_por_biopsia': {'nulos': 0, 'infinitos': 140}}` y `3) Filas no finitas: 140` con `num_biopsias_previas = 0` en las 140. `validar_datos` (definida en celda [10]) nunca se llama en el flujo de entrenamiento real: celda [18] (id=93d6fc8f) ejecuta `df = cargar_datos(); df = construir_caracteristicas(df); modelo, acc_train, acc_test = entrenar_modelo(df)` sin invocar `validar_datos`. Resultado: celda [18], salida: `ValueError: Input X contains infinity or a value too large for dtype('float64').` | Coincide exactamente con el caso de FALLA de la Skill: función de validación definida pero nunca llamada en el pipeline real; el `fit` recibe valores infinitos y falla. |
| V6.1 | Sin identificadores ni atributos prohibidos | PASA | `COLUMNAS_PREDICTORAS` (celda [16], id=881f59c8): `mean texture`, `mean smoothness`, `mean symmetry`, `mean fractal dimension`, `texture error`, `smoothness error`, `symmetry error`, `variabilidad_por_biopsia`, `num_biopsias_previas`, `sesiones_tratamiento_programadas`. Ninguna es un identificador único, y no hay archivo de apoyo que declare algún atributo prohibido. | Ninguna. (`sesiones_tratamiento_programadas` ya se evalúa como fuga en V3.2, no como identificador o atributo prohibido). |

## Acciones recomendadas

1. **[V1.1]** Corregir primero el error de V5.1 para que el pipeline termine de ejecutarse y las métricas queden en una salida guardada; sin eso no se puede verificar el rango de ninguna métrica. (prioridad: media)
2. **[V1.2]** Agregar el cálculo y la impresión de una matriz de confusión (`confusion_matrix`) en `entrenar_modelo` o en la celda del pipeline, para poder auditar la consistencia de las métricas. (prioridad: media)
3. **[V1.3]** Reportar métricas por clase (p. ej. `classification_report` o `precision_recall_fscore_support` con `pos_label` explícito) para poder verificar que la clase maligna (0) se evalúa con su propia etiqueta. (prioridad: media)
4. **[V1.4]** Además de accuracy, calcular y reportar exhaustividad y precisión (o F1) de la clase maligna, y la matriz de confusión o la línea base de la clase mayoritaria, dado el desbalance de 1.68. (prioridad: alta)
5. **[V1.5]** Declarar explícitamente, en una celda de texto o en un archivo de apoyo, qué error es más costoso (probablemente el falso negativo: decir "benigno" cuando el tumor es maligno) y usar una métrica prioritaria acorde (exhaustividad de la clase maligna). (prioridad: alta)
6. **[V2.2]** Crear el generador aleatorio dentro de `cargar_datos` (p. ej. `rng = np.random.RandomState(RANDOM_STATE)` como primera línea de la función) en vez de usar un `RandomState` global compartido entre las tres llamadas a la función. (prioridad: alta)
7. **[V2.3]** Agregar `stratify=y` a la llamada de `train_test_split` en `entrenar_modelo`, dado que el problema es de clasificación binaria y está desbalanceado. (prioridad: alta)
8. **[V2.4]** Si se pretende comparar `usar_class_weight=True` contra `False` (el parámetro ya existe en la función), invocar `entrenar_modelo` con ambos valores sobre la misma partición y reportar ambos resultados; si no se pretende comparar, se puede dejar así. (prioridad: media)
9. **[V3.2]** Eliminar `sesiones_tratamiento_programadas` de `COLUMNAS_PREDICTORAS` (o de raíz, dejar de construirla a partir de `diagnostico`): es una fuga directa de la variable objetivo y por sí sola vuelve trivial la predicción. (prioridad: alta — crítica)
10. **[V4.1]** Si en el futuro se agrega algún atributo protegido o proxy (p. ej. edad de la paciente), calcular la métrica prioritaria por subgrupo antes de desplegar el modelo. (prioridad: media)
11. **[V4.2]** Depende de V4.1: una vez calculada la métrica por subgrupo, verificar que ningún subgrupo con ≥20 positivos se desvíe más de 0.10 de la métrica global. (prioridad: media)
12. **[V5.1]** Proteger la división en `construir_caracteristicas` (p. ej. reemplazar `num_biopsias_previas == 0` por un valor mínimo de 1, o usar `np.where(df['num_biopsias_previas'] == 0, np.nan, df['mean texture'] / df['num_biopsias_previas'])` seguido de imputación dentro de un pipeline ajustado solo con entrenamiento), y llamar a `validar_datos` dentro del flujo real de `entrenar_modelo` o del pipeline de la celda [18], no solo en una celda de diagnóstico aislada. (prioridad: alta)

## Limitaciones de esta auditoría

- El notebook no tiene salidas guardadas de ninguna métrica de desempeño (accuracy, precisión, exhaustividad, F1, AUC) ni de matriz de confusión, porque la celda del pipeline (celda [18]) falla con `ValueError` antes de imprimirlas. Esto deja V1.1, V1.2 y V1.3 en NO SE PUEDE DETERMINAR.
- Los campos `execution_count` embebidos en las salidas muestran que `cargar_datos` (celda [4]) se ejecutó como la 18ª ejecución del kernel y `construir_caracteristicas` (celda [12]) como la 22ª, mientras que la celda de diagnóstico (celda [10], id=fbef5102) — que aparece *antes* de la definición de `construir_caracteristicas` en el orden del documento pero ya la usa — solo pudo ejecutarse *después* de la 22ª ejecución. Esto confirma que las salidas guardadas provienen de una ejecución fuera de orden, no de una corrida lineal de arriba hacia abajo: un "Reiniciar y ejecutar todo" sobre el notebook tal como está ordenado fallaría antes, en la celda [10], con `NameError: name 'construir_caracteristicas' is not defined`, antes de siquiera llegar al `ValueError` documentado en la celda [18]. No se puede confirmar que las salidas reflejen una única corrida coherente de principio a fin.
- No se auditó ningún archivo adicional (`app.py`, `test_app.py`, los `.docx` de las tareas) porque la entrada de esta auditoría fue el `.ipynb` puntual, y el notebook no importa ningún archivo local.
